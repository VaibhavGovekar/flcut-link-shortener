// prisma/schema.prisma

datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Link {
  id        String   @id @default(cuid())
  longUrl   String
  shortCode String   @unique // This @unique rule prevents duplicate links at the database level!
  createdAt DateTime @default(now())
}